/** @type {import('tailwindcss').Config} */
module.exports = {
  purge: [],
  darkMode: false, // or 'media' or 'class'
  theme: {
    extend: {
      // You can extend the default theme and customize it here.
      // For example, you can add additional colors, fonts, or spacing options.
    },
  },
  variants: {
    extend: {
      // You can extend or customize the default variants for utilities here.
      // For example, you can control the hover, focus, or active states of elements.
    },
  },
  plugins: [
    // You can include additional plugins here, such as custom components or utilities.
  ],
}
